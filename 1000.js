function solution(nums, k) {
  let l = 0;
  let r = k - 1;
  let ret = 0;
  const len = nums.length;

  while (r < len) {}

  return ret;
}

// let nums, k;
// (nums = [1, 4, 2, 10, 2, 3, 1, 0, 2]), (k = 4);

// const ret = solution(nums, k);
